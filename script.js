// Area of a triangle = (1/2)bh

function areaOfTriangle (b, h) {
    let base = b
    let height = h

    result = (1/2 * base * height)
    return result 
;}

console.log(areaOfTriangle(5, 2))